# ── Copiá este archivo a `.env` dentro de frontend/ ─────────────────────────
# (el `.env` real está en .gitignore y NO debe commitearse)

# URL del backend Flask.
# En local, dejala vacía o apuntala a http://localhost:5001.
# En producción, poné la URL pública del backend (Railway), p. ej.:
#   FLASK_URL=https://api.tupeluqueria.com
FLASK_URL=http://localhost:5001
